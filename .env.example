# Runtime values are set in Sites, never exposed as VITE_ variables.
INTAKE_FEED_URL=https://bearagon.com/api/intake/feed
INTAKE_FEED_TOKEN=

# Stored as a secret in the Bearagon Ops Site settings.
GEMINI_API_KEY=
